#include <iostream>
#include <cstring>
#include <cstdio>
using namespace std;
 
int main()
{ char a[1001];int n,i,j=0,p=0;
    gets(a);
    n=strlen(a);
    for(i=0;i<n;i++)
       if(a[i]!=' ' || (a[i]==' ' && a[i+1]!=' '))
          cout<<a[i];
 
    return 0;
}
